<div class="p-10 grid grid-cols-1 sm:grid-cols-1 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-3 gap-5">
  <div class="max-w-lg mx-auto mt-16 text-center max-w-sm rounded overflow-hidden shadow-lg p-10">
    <h1 class="mb-4 text-3xl font-black"><%= @supplier.name %></h1>
    <p class="text-lg leading-snug"><%= @supplier.email %></p>
  </div>
  <% @products.each do |product| %>
    <%= product.inspect %>
  <% end %>
  <div class="px-6 pt-4 pb-2">
  <%= link_to '<button class="bg-blue-500 hover:bg-blue-700 text-white font-bold w-1/3 lg:w-full py-2">Create New Product</button>'.html_safe, new_catalogue_supplier_product_path(@supplier.id) %>
</div>

</div>